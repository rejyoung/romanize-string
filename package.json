{
    "name": "romanize-string-monorepo",
    "private": "true",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "tsc -b",
        "clean": "tsc -b --clean"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
