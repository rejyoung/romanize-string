{
    "name": "romanize-string-monorepo",
    "private": "true",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "tsc -b",
        "clean": "tsc -b --clean",
        "publish:romanize-string": "pnpm publish --filter romanize-string --access public"
    },
    "devDependencies": {
        "@changesets/cli": "^2.29.5",
        "npm-run-all2": "^8.0.4"
    }
}
